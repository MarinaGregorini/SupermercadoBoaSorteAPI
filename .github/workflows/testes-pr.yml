jobs:
  zap-security-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Preparar diretórios e criar scripts
        run: |
          mkdir -p zap-output/scripts
          chmod -R 777 zap-output

          cat << 'EOF' > zap-output/scripts/post_criar_consumidor.js
          function sendingRequest(msg, initiator, helper) {
            if (msg.getRequestHeader().getMethod() === "POST" &&
                msg.getRequestHeader().getURI().toString().endsWith("/api/consumidores/")) {
              var body = '{"username":"testuser","email":"testuser@example.com"}';
              msg.setRequestBody(body);
              msg.getRequestHeader().setHeader("Content-Type", "application/json");
              msg.getRequestHeader().setContentLength(body.length);
            }
          }
          EOF

          cat << 'EOF' > zap-output/scripts/post_escolher_produtos.js
          function sendingRequest(msg, initiator, helper) {
            var uri = msg.getRequestHeader().getURI().toString();
            if (msg.getRequestHeader().getMethod() === "POST" &&
                uri.endsWith("/api/consumidores/1/produtos/")) {
              var body = '{"produtos":[1]}';
              msg.setRequestBody(body);
              msg.getRequestHeader().setHeader("Content-Type", "application/json");
              msg.getRequestHeader().setContentLength(body.length);
            }
          }
          EOF

      - name: Criar ficheiro zap.yaml
        run: |
          cat << 'EOF' > zap-output/zap.yaml
          env:
            contexts:
              - name: PublicContext
                urls:
                  - http://50.85.110.248
                includePaths:
                  - http://50.85.110.248/api/.*
          jobs:
            # Carregar scripts
            - type: script
              name: Load Script POST Criar Consumidor
              parameters:
                scriptName: post_criar_consumidor.js
                scriptType: standAlone
                scriptFile: scripts/post_criar_consumidor.js
                action: add

            - type: script
              name: Load Script POST Escolher Produtos
              parameters:
                scriptName: post_escolher_produtos.js
                scriptType: standAlone
                scriptFile: scripts/post_escolher_produtos.js
                action: add

            # Executar scripts
            - type: script
              name: Run Script POST Criar Consumidor
              parameters:
                scriptName: post_criar_consumidor.js
                scriptType: standAlone
                action: run

            - type: script
              name: Run Script POST Escolher Produtos
              parameters:
                scriptName: post_escolher_produtos.js
                scriptType: standAlone
                action: run

            # Active scan sem context, usa contextName se necessário
            - type: activeScan
              name: Active Scan on Public Endpoints
              parameters:
                contextName: PublicContext

            - type: report
              parameters:
                reportDir: /zap/wrk/
                reportTitle: "ZAP Scan Report"
                template: traditional-html
                reportFile: zap-report.html
          EOF

      - name: Executar ZAP Automation Framework
        run: |
          docker run --rm \
            -v $(pwd)/zap-output:/zap/wrk/:rw \
            ghcr.io/zaproxy/zaproxy \
            zap.sh -cmd -autorun /zap/wrk/zap.yaml

      - name: Analisar relatório gerado
        run: |
          if grep -q "FAIL" zap-output/zap-report.html; then
            echo "❌ Vulnerabilidades encontradas. Verifica o relatório."
          else
            echo "✅ Nenhuma vulnerabilidade grave detetada."
          fi
#