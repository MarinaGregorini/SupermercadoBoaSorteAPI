{{- if .Values.grafana.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboards
  namespace: {{ .Values.namespace }}
data:
  python-app.json: |-
    {
      "title": "Python App Metrics",
      "panels": [...]
    }
{{- end }}